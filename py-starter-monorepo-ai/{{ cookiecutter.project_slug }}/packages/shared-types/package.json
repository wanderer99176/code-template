{
  "name": "@{{ cookiecutter.project_slug }}/shared-types",
  "version": "{{ cookiecutter.version }}",
  "description": "共享的 TypeScript 类型定义",
  "main": "index.ts",
  "types": "index.ts",
  "private": true,
  "scripts": {
    "generate": "openapi-typescript openapi.json -o types.ts",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "openapi-typescript": "^6.7.4",
    "typescript": "^5.3.3"
  }
}

